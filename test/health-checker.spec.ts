import assert from 'assert';

describe('health checker test', () => {
    context('Test context', () => {
        it('should assert true to confirm that tests works', () => {
            assert(true, "Tests works !")
        });
    });
});
